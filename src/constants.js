const CMD = {
    SHOW_CHART: 'vscrypto.cmd.showChart',
    SEARCH: 'vscrypto.cmd.search',
    CLEAR: 'vscrypto.cmd.clear',
    UPDATE_TREE_VIEW: 'vscrypto.cmd.updateTreeView'
}

const View = {
    VS_CRYPTO_VIEW: 'vscrypto-view',
    VS_CRYPTO_WEBVIEW: 'vscrypto-webview'
}

const SCHEME = {
    VS_CRYPTO: 'vs-crypto'
}

const CONFIG = {
    EXCHANGE: 'vscrypto.exchange',
    CHART_BACKGROUND_COLOR: 'vscrypto.view.backgroundColor'
}

const CONTEXT = {
    SEARCH_TEXT: 'vscrypto.searchText'
}

module.exports = {
    CMD,
    View,
    SCHEME,
    CONFIG,
    CONTEXT
}